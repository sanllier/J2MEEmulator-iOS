package javax.microedition.lcdui;

// Stub — used by Alert.setIndicator
public class Indicator extends Gauge {
    public Indicator(int maxValue, int initialValue) {
        super(null, false, maxValue, initialValue);
    }
}
